specVersion: "v1beta"
serviceId: "tradersplayground"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tradersplayground-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
